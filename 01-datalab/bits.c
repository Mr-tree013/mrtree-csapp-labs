#include "bits.h"

/*
 * bitAnd - x & y using only ~ and |
 * Example: bitAnd(6, 5) = 4
 * Legal ops: ~ |
 * Max ops: 8
 */
int bitAnd(int x, int y) {
  // TODO: implement
  return 0;
}

/*
 * bitXor - x ^ y using only ~ and &
 * Example: bitXor(4, 5) = 1
 * Legal ops: ~ &
 * Max ops: 14
 */
int bitXor(int x, int y) {
  // TODO: implement
  return 0;
}

/*
 * thirdBits - return word with every third bit (starting from LSB) set to 1
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 8
 */
int thirdBits(void) {
  // TODO: implement
  return 0;
}

/*
 * fitsBits - return 1 if x can be represented as an n-bit two's complement int
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 15
 */
int fitsBits(int x, int n) {
  // TODO: implement
  return 0;
}

/*
 * sign - return 1 if positive, 0 if zero, -1 if negative
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 10
 */
int sign(int x) {
  // TODO: implement
  return 0;
}

/*
 * getByte - extract byte n from word x (n=0 is LSB)
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 6
 */
int getByte(int x, int n) {
  // TODO: implement
  return 0;
}

/*
 * logicalShift - shift x to the right by n, using a logical shift
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 20
 */
int logicalShift(int x, int n) {
  // TODO: implement
  return 0;
}

/*
 * addOK - determine if x+y can be computed without overflow
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 20
 */
int addOK(int x, int y) {
  // TODO: implement
  return 0;
}

/*
 * bang - compute !x without using !
 * Example: bang(3) = 0, bang(0) = 1
 * Legal ops: ~ & ^ | + << >>
 * Max ops: 12
 */
int bang(int x) {
  // TODO: implement
  return 0;
}

/*
 * conditional - same as x ? y : z
 * Example: conditional(2, 4, 5) = 4
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 16
 */
int conditional(int x, int y, int z) {
  // TODO: implement
  return 0;
}

/*
 * isPower2 - return 1 if x is a power of 2, 0 otherwise
 * Legal ops: ! ~ & ^ | + << >>
 * Max ops: 20
 */
int isPower2(int x) {
  // TODO: implement
  return 0;
}

/*
 * float_neg - return bit-level equivalent of -f
 * Legal ops: integer/unsigned ops; if/while allowed
 * Max ops: 10
 */
unsigned float_neg(unsigned uf) {
  // TODO: implement
  return 0;
}

/*
 * float_i2f - return bit-level equivalent of (float) x
 * Legal ops: integer/unsigned ops; if/while allowed
 * Max ops: 30
 */
unsigned float_i2f(int x) {
  // TODO: implement
  return 0;
}

/*
 * float_twice - return bit-level equivalent of 2*f
 * Legal ops: integer/unsigned ops; if/while allowed
 * Max ops: 30
 */
unsigned float_twice(unsigned uf) {
  // TODO: implement
  return 0;
}
